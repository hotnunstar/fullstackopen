{
  "name": "backend",
  "version": "1.0.0",
  "description": "Blog List Project",
  "main": "index.js",
  "scripts": {
    "test": "echo \\\"Error: no test specified\\\" && exit 1",
    "start": "node index.js",
    "dev": "nodemon index.js",
    "build:ui": "rm -rf dist && cd ../frontend/ && npm run build && cp -r dist ../backend",
    "lint": "eslint ."
  },
  "author": "Nuno Araújo",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "mongoose": "^8.3.4"
  },
  "devDependencies": {
    "eslint": "^9.2.0",
    "nodemon": "^3.1.0"
  }
}
